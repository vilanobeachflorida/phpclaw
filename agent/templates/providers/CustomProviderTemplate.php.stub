<?php

namespace App\Libraries\Providers;

/**
 * {{PROVIDER_NAME}} - Custom provider adapter.
 *
 * Generated by: php spark agent:provider:scaffold {{PROVIDER_SLUG}}
 *
 * Implement the required methods below to integrate your LLM provider.
 */
class {{PROVIDER_CLASS}} extends BaseProvider
{
    protected string $name = '{{PROVIDER_SLUG}}';
    protected string $description = '{{PROVIDER_DESCRIPTION}}';

    protected function getDefaultConfig(): array
    {
        return [
            'base_url' => 'http://localhost:8000',
            'api_key_env' => '{{PROVIDER_UPPER}}_API_KEY',
            'default_model' => 'default',
            'timeout' => 120,
        ];
    }

    public function getCapabilities(): array
    {
        return [
            'chat' => true,
            'streaming' => false,
            'system_prompt' => true,
            'model_list' => false,
        ];
    }

    public function healthCheck(): array
    {
        // TODO: Implement health check for {{PROVIDER_NAME}}
        // Example: ping the provider endpoint
        return ['status' => 'ok', 'provider' => $this->name];
    }

    public function listModels(): array
    {
        // TODO: Implement model listing if supported
        return [];
    }

    public function chat(array $messages, array $options = []): array
    {
        $model = $options['model'] ?? $this->config['default_model'];

        // TODO: Implement chat completion
        // 1. Build the request payload from $messages
        // 2. Send to provider API
        // 3. Parse response
        // 4. Return using $this->successResponse() or $this->errorResponse()

        return $this->errorResponse('{{PROVIDER_CLASS}}::chat() not yet implemented');
    }
}
